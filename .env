# ══════════════════════════════════════════════════════════
# PANIC Pasta — Shared Defaults (committed, NO secrets)
# ══════════════════════════════════════════════════════════
# This file is loaded in ALL environments.
# Environment-specific values override these in
# .env.development / .env.production (committed) and
# .env.*.local (gitignored, for secrets).

# Database placeholders (overridden by .env.development.local / .env.production.local)
# Prisma CLI reads .env directly — these must exist for `prisma validate` to pass.
DATABASE_URL="postgresql://placeholder:placeholder@localhost:5432/postgres"
DIRECT_URL="postgresql://placeholder:placeholder@localhost:5432/postgres"

LLM_MODEL=gpt-4o-mini
LLM_MAX_TOKENS=4096
EMAIL_SERVER_PORT=465
EMAIL_SERVER_USER=resend
